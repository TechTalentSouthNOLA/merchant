<%= render 'search_form' %>

<% if params[:category_id] %>
  <h1>All <%= @category.name.titleize %></h1>
<% else %>
  <h1>All Products</h1>
<% end %>

<div class="row">
  <%= render @products %>
</div>
